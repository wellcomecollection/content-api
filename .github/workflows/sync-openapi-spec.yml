name: Sync OpenAPI spec for developers.wellcomecollection.org

on:
  push:
    branches: [main, zod-openapi]
    paths:
      - 'api/**'
  workflow_dispatch:

jobs:
  sync-openapi-spec:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read

    steps:
      - name: Generate GitHub App Token
        id: app-token
        uses: actions/create-github-app-token@v3
        with:
          client-id: ${{ vars.SYNC_APP_CLIENT_ID }}
          private-key: ${{ secrets.SYNC_APP_PRIVATE_KEY }}
          repositories: developers.wellcomecollection.org

      - name: Checkout content-api
        uses: actions/checkout@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install Node
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Install dependencies
        run: yarn install

      - name: Generate OpenAPI spec
        run: npx tsx api/scripts/generate-openapi.ts > /tmp/content.yaml

      - name: Checkout developers.wellcomecollection.org
        uses: actions/checkout@v6
        with:
          repository: wellcomecollection/developers.wellcomecollection.org
          token: ${{ steps.app-token.outputs.token }}
          path: developers.wellcomecollection.org

      - name: Copy generated spec
        run: cp /tmp/content.yaml developers.wellcomecollection.org/reference/content.yaml

      - name: Check for changes
        id: changes
        run: |
          cd developers.wellcomecollection.org
          if git diff --quiet; then
            echo "changes=false" >> $GITHUB_OUTPUT
          else
            echo "changes=true" >> $GITHUB_OUTPUT
          fi

      - name: Create Pull Request
        if: steps.changes.outputs.changes == 'true'
        run: |
          cd developers.wellcomecollection.org

          # Configure git
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          # Create branch
          TIMESTAMP=$(date +%Y%m%d-%H%M%S)
          BRANCH_NAME="auto-sync/openapi-spec-$TIMESTAMP"
          git checkout -b $BRANCH_NAME

          # Commit and push
          git add reference/content.yaml
          git commit -m "Update OpenAPI spec from content-api"
          git push origin $BRANCH_NAME

          # Open PR
          PR_NUMBER=$(curl -s -X POST \
            -H "Authorization: Bearer ${{ steps.app-token.outputs.token }}" \
            -H "Accept: application/vnd.github.v3+json" \
            https://api.github.com/repos/wellcomecollection/developers.wellcomecollection.org/pulls \
            -d '{
              "title": "🤖 Auto-sync: Update OpenAPI spec",
              "head": "'"$BRANCH_NAME"'",
              "base": "main",
              "body": "This PR updates `reference/content.yaml` from the generated OpenAPI spec in `wellcomecollection/content-api`.\n\n**Triggered by commit:** ${{ github.sha }}\n\nPlease review and merge if the changes look correct."
            }' | jq -r '.number')

          # Request review from digital-experience team
          curl -s -X POST \
            -H "Authorization: Bearer ${{ steps.app-token.outputs.token }}" \
            -H "Accept: application/vnd.github.v3+json" \
            https://api.github.com/repos/wellcomecollection/developers.wellcomecollection.org/pulls/$PR_NUMBER/requested_reviewers \
            -d '{"team_reviewers": ["digital-experience"]}'
